# CubeEngine：AI 工程師下一輪工作說明

更新：2026-09-24。以已推送的最新 `SRTP` 為整合基準，在工程師的 `llm` 分支開發，再合併回 SRTP。另須取得本次新增的字體／診斷修復；本次修復尚未由 Codex 提交或推送，不應假定已在遠端。

## 工作目標

**由工程師負責把真實 LLM 轉換管線調通，並完成井字棋、掃雷、2048 的實際遊玩與品質驗收。** 不能再由產品設計師一次次付費呼叫、發現 blocker、轉交錯誤。

必須跑通：完整2D遊戲源码 → Compile → Approve Source → 簡短自然語言 Spatial Lift → Approve Target → Transformed 3D 可玩。品質對齊現有手寫成功案例；外觀保留應默認生效，不能要求使用者用長 Prompt 補完技術規格。

## 已有基礎，不要重做

目前是 Rule → Asset → Scene → Input 分階段生成、執行檢查、局部修復及缓存；已有共用 OpenRouter 客戶端、跨檔源码讀取，以及四 IR 驅動的 Ursina 後端。

本地已補整體體積、格壳與內容分離、可見棋子、真實輸入、重開、session 隨機及回放。通用 Prompt、函數說明、修復反例和離線測試也已接入。**本輪不再派一套泛泛的「改 Prompt、加測試」工作；先實測現有版本，只針對尚未解決的真實失敗開發。**

| 最新實測 | 已由 Codex 處理 | 工程師接續驗證 |
| --- | --- | --- |
| 井字棋看不到落子 | 棋子縮放、高亮遮擋及提示覆蓋已修；本次原包可落子、判贏、重開 | 新生成結果是否穩定可玩，整體外觀／交互是否達到原生標準 |
| 2048 | 隨機種子已修；最新真實呼叫 Rule 已通過。目前字體索引已補 `SysFont`＋JSON配置中的 Verdana Bold，使用本機原字體，不替換字型 | 用新資源證據恢復 Asset，繼續 Scene／Input／Lift，找出並修復後續真實阻斷 |
| 掃雷診斷崩潰 | `InvariantDiagnostic` 已轉成可保存／可反饋格式；測試 setup 也會先驗規則一致性 | 最新模型 fixture 額外加雷，使10顆變11顆；讓模型修正測試情境，繼續核驗首步保護、揭格、展開、勝負與計時 |

以上是引擎修復和回放結論，**不是三款新模型管線已全部通過**。

## 工程師下一輪負責三件事

### 1. 接手真實 API 聯調，把未通的管線跑通

在自己的開發環境與明確費用預算內，先恢復上述已付費斷點，逐款推進到 Source／Target／PLAY。每次失敗保留原始模型回應、實際送入的上下文、診斷及最小重現。

工程師負責判斷並解決：模型沒讀懂源码、Prompt／範例不足、階段間資料缺失、模型誤用能力，或仍有引擎缺口。能在該分支修的直接修並加回歸；需要產品決策的，只提交整理好的選項和效果對照，不把每條報錯原樣丟給使用者。

### 2. 根據真實失敗，人工校準 LLM 的轉換方法

閱讀失敗模型輸出和原源码，對照手寫成功案例，調整生成指令、上下文選擇、通用範例或呼叫編排。每項修改必須對應具體反例，並比較修改前後輸出，確認改善能用於其他遊戲或源码變體。

重點：掃雷能否完整重建算法；2048能否正確生成規則、素材和輸入；Spatial Lift 是否真正升維並保留外觀。若模型反覆誤解能力，修模型能看到的用法與反饋；若後端確實不支持，補通用能力。不得直接改最終 Manifest、套手寫遊戲，或放寬檢查來製造成功。

### 3. 人工驗收完整遊戲，把實際體驗缺陷修到交付

在 Workbench／Ursina 連續實際操作，對照成功案例觀察：點擊／鍵盤是否生效、內層能否選取、棋子和狀態是否清楚、勝負／重開是否完整、素材與界面是否保留。離屏截图或規則測試不能替代這一步。

最低範圍：井字棋整體3×3×3及49條勝線；掃雷首步保護、三維鄰域／展開及完整狀態；2048各軸合併、無效移動與生成規則。空間規則的重要默認選擇要記錄，外觀及操作與參考不一致要修或明確列出待決事項。

## 分工與交付

- **Codex 已知且可本地重現的修復，不重複派工。** 本次 JSON 診斷、系統字體等問題由 Codex 完成；後續也可提供離線定位／程式修復支援。
- **工程師承擔持續真實模型調試與人工遊玩驗收的結果責任。** 這是本輪責任分工，不是聲稱這些程式只能由人類撰寫。取得明確 API 預算後受控聯調，同錯停止並先離線分析。
- **交付：** 可合併的程式修改；三款真實新生成完整包；Workbench全流程遊玩錄影；一頁結果表，列出剩餘差距、請求數、token和費用。調試可復用付費缓存，最終完整生成驗收需另做新作業、不復用舊產物。

完成判定：**三款均有真實端到端生成和遊玩證據，達到上述最低範圍；只交 Prompt、測試數字或一份能打開的包，不算完成。**

## 必要資料

- 修改入口：`srtp/llm_compiler_v1/`；呈現／輸入：`srtp/ursina_scene_backend.py`、`srtp/project_viewer.py`。
- 最新現場：`tests/fixtures/field_runs_20260924/`；回歸：`tests/test_font_and_diagnostic_failures.py`。上一輪現場：`tests/fixtures/field_runs_20260923/`。
- 離線基線：`python -m tests.run_offline_acceptance`，不等於真實模型驗收。
- 原生井字棋：`E:/CubeEngine/Dev/3D/tictactoe3d_ursina_v1.py`；掃雷／2048效果：`srtp/transformed_viewer.py`、`srtp/presentation_bridge.py`。
