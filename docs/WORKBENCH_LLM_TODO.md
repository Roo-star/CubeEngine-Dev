# Workbench / LLM 待办

更新：2026-09-23。开发目录：`E:/CubeEngine/CubeEngine-SRTP`，开发分支：`SRTP`。
`llm` 是工程师交付分支，只拉取和测试；本地修复及更新均进入 `SRTP`。

## 当前开发主线：完成 Workbench 内的自动游戏转换

### 2026-09-24：新增現場修復與交接範圍

- [x] 掃雷 `InvariantDiagnostic` 進入 Prompt／報告前標準化，保留真實規則錯誤；fixture 前置 invariant 檢查揭示原付費測試增加第11顆雷，不再以 JSON 崩潰遮蔽。
- [x] 2048 已知 SysFont＋JSON配置需求索引到本機 Verdana Bold；原字體哈希、可攜帶資源、缺字體前置檢查與模型說明已接通。
- [x] 34項針對性與全量469項驗收：468通過、1付費跳過、0失敗；4渲染探針通過，0次API。本次修復尚未提交／推送。見 [修復記錄](FONT_INVARIANT_FIX_20260924.md)。
- [ ] 工程師取得本次修復後，負責真實 API 持續聯調、模型失敗調校及人工遊玩／品質驗收；不重複派發已知的本地程式修復。掃雷 fixture 修正及2048 Asset之後流程尚未取得新模型結果。

前一輪更新已由使用者推送至 SRTP；9月24日本次新增修復仍需同步。下一輪以 [AI 工程師工作說明](AI_ENGINEER_NEXT_ROUND_HANDOFF_20260923.md) 為準：取得完整最新基準後，重點完成井字棋、掃雷、2048 的真實生成與遊玩驗收。

### 2026-09-23：三游戏最新实测修复

- [x] 新井字棋真实 Target：修复格心棋子被载体缩小 100 倍、黄色高亮实心遮挡、未绑定释放覆盖交互提示；原包不改通过 27 格射线选取、可见落子、三维胜负/回放/重开。
- [x] 2048 session 随机种子由宿主统一提供，覆盖编译、Scene/Input 验收、项目打开和回放；修复比较计数/coalesce 降低与被忽略的测试棋盘。
- [x] 扫雷读取/解析路径：区分 final 与 commentary、只读请求安全合并、空结束请求、保留已读依赖、解析失败原文留证。原错误文本旧版未保存，具体 Extra data 形态尚不能追认。
- [x] 462 项离线测试：461 通过、0 失败、1 付费跳过；4 渲染探针及本次新井字棋包的额外渲染检查通过。API 0 次。见 [实测修复与边界](THREE_GAME_FIELD_FIX_20260923.md)。
- [ ] 2048 原回应误用合并倍率 4，独立于 seed 问题；现在本地可重现并正确拦截，生成契约已明确参数，尚待模型修复结果。
- [ ] 扫雷原回应缺少揭格/首步保护/胜负/计时；完善方法契约后仍须实际新生成验收，不能宣称仅修 JSON 即完整可玩。

### 2026-09-23：以原生成功案例为完整质量标准

- [ ] **总目标尚未完成：B（Workbench 内真实新转换）≥ A（原生成功案例）。** 旧 Manifest 回归、整体 Cube、编译成功均不等于该目标。见 [参考案例审查与当前差距](REFERENCE_PARITY_REVIEW_20260923.md)。
- [x] 阅读用户原生井字棋及扫雷/2048 适配器的具体方法；提炼透明格壳/实体内容/源贴面/数字配色契约；修复统一透明立方体覆盖原 mesh/贴图的后端问题；实体棋子格心呈现，紧凑单位间距。
- [x] 生成契约与后端共享呈现角色，加入一致性预检；新增独立质量报告，未验证的玩法/视觉/生命周期明确 pending；不将编译成功写作产品通过。
- [x] 保存的真实模型井字棋 Target 与用户原生逻辑的全部 49 条获胜线对照通过。离线 454 项：453 通过、0 失败/错误、1 付费跳过；4 渲染探针通过。最后小改后 23 项针对性检查再次通过。本轮 0 次 API。
- [x] 准备禁止任何阶段缓存的全新 Source → 自然语言 Lift → 新包渲染联调入口；记录同一产物来源与哈希，失败停止。
- [ ] **尚未执行上述付费新转换**，不得让用户把已有 Manifest 当作新管线证明。冷启动入口准备好不等于实际新生成已通过。
- [ ] 补齐通用 HUD/菜单、完整悬停与终局反馈、悔棋/AI 模式及生成契约；独立规则与视觉状态矩阵、连续游玩性能写入质量结论。九类质量报告目前不是九类自动验证均已完成。

### 2026-09-23：生成契约与执行后端系统审查

- [x] 修复实际生成井字棋 Target 的空白 Ursina：模型相机背向棋盘，新增初始取景验证/恢复；修复 X/O 被格子遮挡、层选择碰撞节点残留、窗口边缘控件裁切。保留原 Manifest/IR，0 次 API；实际生成包的 27 格射线选取、落子显示、三维斜线判赢/重开/视角复位通过。全仓 444 项中 443 通过、0 错误、1 项付费跳过，4 个实际渲染探针通过。见 [空白修复记录](URSINA_BLANK_TARGET_FIX_20260923.md)。
- [x] P0 整体体积语义：横向三个平面是转换失败。新增引擎拥有的 `cubeengine.volume-grid/1`，Spatial Lift 编译/输入验收与 Target 播放共用，原用户 Manifest 不改即可形成整体 Cube；27 格精确布局/独立边框/射线选取/落子/获胜/重开通过，四种尺寸布局回归通过。全仓 448 项中 447 通过、0 失败，4 个渲染探针通过。本轮 API 为 0。见 [体积布局契约](VOLUME_LAYOUT_CONTRACT_20260923.md)。
- [ ] 继续验收不同游戏的完整状态外观、素材保真和特殊空间结构；通用立方体布局通过不等于所有游戏完整转换通过。

- [x] 授权真实联调实际使用 2/8 次请求（US$0.1800529）：Scene 通过，Input 因缺少宿主退出能力失败，按约定停止。已补齐 `host_command` 退出/终局重开、同步生成契约与运行器，保持原失败响应可复现。修改后全量 442 项中 441 通过、0 错误、1 项付费 API 跳过，三个渲染探针通过；另有 2 项离线累计预算测试通过。真实新 Input/Lift 尚未复验，不能标记整条转换通过。

- [x] 后续验收：修复训练工程目录与 tqdm 依赖，真实 MCTS/Coach 自我对弈通过；新增生产 HTTP 适配器贯穿修复/缓存/批准/Lift/三维胜负的离线测试。统一验收 439 项中 438 通过、0 错误、1 项付费 API 跳过，三个实际渲染探针通过。可复现命令 `python -m tests.run_offline_acceptance`，详见 [验证记录](LLM_FIX_VERIFICATION_20260923.md)。

- [x] Source 作业 `5c8a8484fdc94f8b811328db99620b29`：跨 Rule/Asset/Scene/Input 对齐可执行生成契约、批量错误反馈、运行状态与真实输入联检；修复 winner_state 丢失、PageUp、正交相机与 HUD 生命周期问题。保存原付费 Rule/Asset 和原失败 Scene 修复断点，不注入手写成功包。本轮 API 请求为 0。
- [x] 离线全仓 437 项测试：435 通过，1 既有训练集成环境错误，1 真实 API 用例跳过；3 个真实 Ursina 离屏探针通过。详见 [系统审查](LLM_CONTRACT_AUDIT_20260923.md)。
- [ ] 有预算上限的真实 Scene/Input/Lift 集成验收及源游戏独立行为/视觉对照；不把本轮 schema/后端测试作为完整自动转换成功，不要求用户反复从头付费试错。

最新执行方式：由当前 Codex 在 SRTP 统一开发 LLM 编译、执行后端和 Workbench；没有必须先交给外部 AI 工程师的前置工作。源码、用户 Manifest、Inspector 尺寸和自然语言属于同一引擎的输入方式。

本轮实施已落地：源码读取工具、分阶段编译/执行反馈/断点缓存、严格语义失败、通用区域与序列运算、Scene/Asset 驱动 Ursina、Manifest 导入及 Inspector 尺寸转换。详见 [实施记录](LLM_ENGINE_IMPLEMENTATION_20260922.md)。下面整包复选框保持未完成，直到真实模型、多游戏和视觉质量门槛通过；不会把内部 mock 或参考包算作完成。

### 不等待 LLM 服务的引擎侧工作（当前 Codex 负责）

- [x] 旧包外观兼容：运行器支持旧 empty/positive/negative/body/head/food 状态，显示透明格子与立体标记；保留原包哈希。新编译结果仍须给出完整外观定义，未知状态不能兼容兜底。窗口显示兼容外观提示。附带层选择按钮、悬停边框及混合输入事件序号修复。见 [本次修复与缺口](URSINA_COMPATIBILITY_AND_GAPS_20260922.md)。
- [ ] P0 外观还原：建立源素材/图集/字体/颜色到呈现的可追踪映射、完整 HUD、布局、动画和音效；不能把兼容标记当成默认原作外观。
- [ ] P0 通用表达与执行：补齐所需几何/材质配方、生命周期、计时/随机/实体行为、输入功能；未知能力必须可定位，不能靠游戏名路由或隐藏降级。
- [ ] P0 独立验收：原游戏输入轨迹/截图基准，Z=1 规则与外观对照；井字棋、扫雷、2048、Connect、Snake 及源码变体的离线基准。真实生成接入后沿用相同门槛。
- [ ] P1 三维交互质量：完整深度/内部选择引导、拖拽与落子/标记冲突处理、相机恢复、终局呈现、AI 对手运行接入与性能验收，对照用户手写版本。
- [ ] P1 Workbench 任务系统：异步工作、取消、重复点击隔离、旧结果与本次结果清楚区分；让成功结果直接进入可游玩工作流。
- [ ] P1 可移植产物：原资源随包收集、跨目录打开、依赖检查与导出；不依赖开发机的源码绝对路径。

以上不是等待模型服务的理由，均是当前引擎实现应继续完成的内容。LLM 恢复后另做真实端到端生成验收。

### 2026-09-22 本轮逐项实施进度

- [x] 源视觉证据：扫描五类游戏的配色、绘制调用、字体和资源加载；颜色引用由构建器按源文件哈希解析，保存来源。动态表达式不猜值。
- [x] 表现能力增量：RGBA 轮廓挤出（含透明孔洞和原色）、源图集帧动画、状态绑定音效、HUD 字体/颜色/背景接入通用后端；真实 Ursina 离屏渲染和音频加载验证通过。
- [x] 独立算法基准：井字棋 50 组轨迹、2048 的 256 种行、扫雷 12 个展开区域、Connect Four 100 个棋盘、Snake 100 次移动/增长，与原源码对照。
- [x] Workbench 任务基础：后台执行、主线程更新界面、进度/取消/重复点击保护；切换源、改变尺寸或 Intent 隔离旧结果；取消不替换旧成功包。
- [x] 交互增量：拖动后不触发落子/标旗；点击必须起止同一格；恢复初始视角，重开按钮独立于源游戏键盘动作。
- [x] 新编译包携带声明的原始资源；复制时验证哈希；移动包、删除原素材后可通过 ProjectHost 打开和操作，资源损坏拒绝加载。
- [x] 性能基线与修复：未变状态跳过重复投影、只更新变更节点、缓存几何及碰撞体。20×20×5 保存包的静态刷新＋离屏渲染中位数约 238 ms → 15.5 ms；不代表连续游玩帧率。
- [ ] 完整源界面截图/状态矩阵与 Z=1 自动视觉对照，以及全部五款游戏生命周期验证。
- [ ] 动态绘制、完整菜单/HUD 布局、复杂几何/材质的默认还原覆盖；已有原语不等于整款游戏保真已达标。
- [ ] 当前 Project IR 游玩入口中的 AI 对手选择、训练权重与回放接入，并与手写 Ursina 标准对照。
- [ ] 真实模型多游戏端到端转换与持续游玩性能验收；本轮没有发起模型调用。

详细实现、测试范围和剩余工作见 [引擎侧实施记录](ENGINE_CONVERSION_PROGRESS_20260922.md)。上方整包复选框仍保留未完成，不把能力原语和离线参考测试算成完整产品验收。

2026-09-22 CTO 审查后重排。以 [自动转换技术决策与工程工单](E:/CubeEngine/CubeEngine-SRTP/docs/LLM_CONVERSION_CTO_DECISION_20260922.md) 为当前执行依据。下面旧问题记录不代表原先的优先级继续有效。参考包运行、Target 启动优先级和通用立方体预览不作为自动转换验收。

- [ ] W0：禁止未知规则表达式静默变为常量合法性；以行为、交互、外观定义编译成功。
- [ ] W1：修复默认证据包删除所有源码片段的问题；实现跨文件按需读取、运行观察、分阶段编译与局部修复。
- [ ] W2：实现小型带类型的游戏构建 API 与确定性 IR 编译；以井字棋连线、扫雷展开、2048 合并验证表达能力；缺失能力不能靠硬编码游戏或静默降级绕过。
- [ ] W3：默认恢复原素材、字体、配色和状态反馈；实现真实消费 Scene/Asset 的 Ursina 后端，与规则工作同步推进。现有手写游戏作为质量标准。
- [ ] W4：在 Workbench 内从完整二维井字棋源码完成真实模型转换及三维游玩；无人手改/附加中间 Manifest 或目标规则；达到用户提供的 Ursina 井字棋标准。
- [ ] W5：扫雷、2048、Connect Four、Snake 全流程与独立源码变体回归；自开始就建立多游戏测试，避免井字棋专用实现。
- [ ] W6：完整可重复工作流上比较模型的通过率、耗时和费用。服务准入修复保留，但加 Key/换模型不是编译能力开发的替代。

本轮审查证据：五个游戏默认 evidence 包均未保留 snippet；未知表达式可被归一化为 true；参考 IR 的井字棋 49 条空间获胜线对照通过（直接注入状态，仅证明该项表达能力，非 LLM 转换成功）。见 `LLM_CTO_AUDIT_EVIDENCE_20260922.json`。

## 历史阻断记录：按上述主线归并处理

- [x] 2026-09-23 井字棋 OpenRouter 作业 `4bd3e28219784599a1426e1dfb8f24bf`：修复引用格式冲突、Pygame 默认字体资源缺口、空资源清单判定、失败 unresolved 错位；加入相同阻断停止、任务请求数上限及返回用量记录。核验并保留已付费 Rule 缓存，升级后离线执行复检；本轮未调用 API。见 [根因与验证](LLM_ASSET_PREFLIGHT_FIX_20260923.md)。
- [ ] 上述修复后的真实 Asset→Scene→Input→Lift→3D 游玩验收；尚未收到这些新模型输出，不把离线模拟当作完整转换成功。

- [x] 2026-09-23 按用户要求改用 OpenRouter，保留 GPT-6 Sol（`openai/gpt-6-sol`）。Source、Intent、Lift、源码补读、修复及 CLI 共用入口；迁移配置、错误分类、重试、缓存隔离和文档。120 项离线测试中 119 通过，1 跳过。见 [迁移与操作说明](OPENROUTER_MIGRATION_20260923.md)。
- [ ] 用户填入根目录 `.env` 的 `OPENROUTER_API_KEY` 后，验收真实 Source→批准→Lift→Transformed 3D；本轮未发送真实 API 请求。

- [x] 2026-09-23 OpenAI 429 误分类：用户作业 `f65dcdc28ea9462a8ee838442dff4e71` 只保存笼统限流信息。使用当前 Key 发一次极小请求（无游戏源码、64 输出 token 上限、无重试）返回 `credit_balance_exhausted` / `insufficient_quota`，确认当前阻断是 API 预付余额耗尽。补齐余额、组织/项目支出上限、组织使用限额的分类，并同时检查 code/type，防止具体 code 遮住 insufficient_quota；这类失败不再自动重试。脱敏记录 `.cubeengine_llm/diagnostics/openai_429_20260923.json`。
- 已由 OpenRouter 迁移替代：不再以恢复 OpenAI 余额作为当前验收前置条件；旧 429 记录保留。

- [x] 2026-09-23 按用户要求统一切换 OpenAI：当前产品 Source、Intent、Spatial Lift、源码补读、CLI 共用 OpenAI Responses；删除 Gemini/Groq 回退与 FreeFlow 依赖。默认 gpt-6-sol，根目录 `.env` 预留单个 OPENAI_API_KEY；加入请求/错误/截断/缓存隔离回归。见 `OPENAI_MIGRATION_20260923.md`。
- 原 OpenAI Key 验收任务由上方 OpenRouter 真实验收替代；离线 HTTP 模拟测试不替代真实模型质量验收。

- [x] 2026-09-23 井字棋 Source `3dd9405d03f74b3c8dc5963605904089`：修复生成 schema 与真实类型/命令操作数不一致、补丁只报首错、最终诊断混入已解决错误的问题。共享命令契约、批量静态校验、字面初值类型检查、当前/历史反馈分离及缓存失效已实施。相关 156 项测试中 155 通过、1 跳过；不修改原响应的回放确认原先隐藏的 grid.set 和布尔枚举错误。详见 `LLM_RULE_CONTRACT_FIX_20260923.md`。
- [ ] 上述契约修复后的真实井字棋 Source→Lift→游玩验收：本轮两次 Gemini 请求均返回 high demand，未收到新生成结果；不能把本地测试或旧参考包当成此项已完成。

- [x] 2026-09-23 井字棋 Source `5c08fca253294fb4b4ce631ce13386f4` 的生成契约冲突：Prompt 禁止引擎字段，实际发送的完整 schema 却要求这些字段。改用去除封装字段的生成 schema；收到完整 IR 时只忽略引擎管理字段并重新构建，未知语义仍报错。补充真实表达式操作枚举、简写表达式格式、命令表达式字段和状态读取说明；修复反馈保留历次错误，记录忽略的封装字段。离线原响应回放仍因不合法 actor/命令 target 阻断，不宣称原游戏已编译成功。详见 `LLM_AUTHORING_CONTRACT_FIX_20260923.md`。

- [ ] 完整二维游戏 → LLM 自动转换 → Ursina 完整可玩三维游戏。已有 Project IR 网格主机和路由只是基础能力，不代表该目标完成；后续以 W1–W5 验收。
- [x] 已补充完整 Pygame 井字棋，包含界面与主循环，加入 Source Library；通过落子/胜负/平局/重开测试及主循环启动检查。
- [x] 移除通用校验对非空初始棋盘的要求；允许井字棋空棋盘。提示仍需持续去除其余 Snake 专用假设。
- [ ] 扫雷 unhashable dict 现场复测：已加状态 ID/类型防护，以及归一化/补丁类型错误进入诊断与修复的边界；原始报错没有堆栈，不能宣称已定位该次具体字段。
- [x] 修复 Lift 新增 Z 动作遗漏 actor/timing：按既有输入动作的统一执行者与阶段继承，不混入系统推进；歧义时不猜测。Source 摘要补齐 actor/timing/真实 effects，校验失败进入有上限的模型修复。现场 f80f45065a3b434d9bde737ccc1bac16 重放消除 actor 报错；93 项相关测试中 92 通过、1 跳过。
- [ ] 实测修复后的 z=5 Lift。旧输出另有 delta-only grid.set；已加入明确诊断与自动修复反馈，旧输出不会被当成可执行玩法。此次离线测试没有发起真实模型调用，完整 Target 成功仍待验证。
- [x] 修复本次 Source 的 `scheduler.clock=timer`：仅在明确 fixed_tick/real_time 模型时转换成对应合法值；补齐提示约束和修复诊断。现场样本经生产转换逻辑离线重放通过四 IR 补丁校验，相关 82 项测试中 81 通过、1 跳过真实 API 测试。等待使用者重启 Workbench 实测。
- [ ] 修复生成的棋盘状态声明：当前样本把棋盘放入 state.grids，缺少 topology_site 状态变量；这是独立可玩性阻断，不能靠批准或放宽校验绕过。见 `LLM_CLOCK_BLOCKER_20260922.md`。
- [ ] **主验收改为井字棋。** 二维井字棋 → 3×3×3 井字棋作为本轮产品示范；Connect Four、扫雷、2048 同轮建立矩阵，Snake 作为回归案例。详见 `LLM_MULTI_GAME_ACCEPTANCE.md`。
- [ ] 修复通用生成提示和可玩性校验中的 Snake 专用假设：空棋盘被误判缺失已通过现有井字棋参考包离线复现；不能靠预放 X/O 绕过。
- [x] 已补齐完整可玩的二维井字棋源项目；此项与上方源游戏交付合并记录，不能将源游戏完成计为转换完成。
- [ ] 服务准入与预检归入 W6；服务恢复后优先处理源码理解、转换和后端能力。`LLM_RECOVERY_PLAN_20260922.md` 中的备用服务方案待实际评测，不作为当前唯一优先路线。
- [ ] 基准 Source/Target IR 仅作内部运行时回归；用户验收必须从完整原始源码发起，禁止以 fixture 替代全流程成果。
- [ ] 保存 API 原始错误码、配额维度、请求 ID、结束原因与 token 使用；地区拒绝不重试，额度问题按具体维度处理。历史 IP 无记录，不能事后猜测。
- [x] 修复失败的 Source 重编译覆盖旧 IR、保留旧 Manifest，导致 Approve 报 missing pinned rule_ir。失败输出隔离，成功包完整替换并保留旧包，审批前校验文件版本；离线回归通过。
- [x] 修复 bundle loader 按文件名兜底绕过 Manifest 哈希匹配，禁止把损坏的包继续送入 Spatial Lift。
- [x] Gemini 繁忙自动进行有上限的退避重试，默认共 3 次、间隔 2/4 秒；耗尽后停止，服务故障不会再进入模型 JSON 修复循环。
- [ ] 服务持续繁忙时验证可用备用模型/服务。目前本地仅配置 Gemini，未配置 Groq；没有自动切换的备用凭据。重试不能保证服务恢复。
- [x] 修复失败 Lift 继承 Source 的 compile_ready=true，避免错误报告把 Source 的状态当成 Target 成功。
- [x] 从现场快照恢复完整真实 Source 草稿到当前工作目录，保留失败现场；恢复结果仍待使用者批准，未代替用户 Approve。
- [ ] 在当前 SRTP 上完成原始完整源码 → 模型编译与验证 → Transformed 3D 完整游玩的实际验收。Approve 流程通过、离线测试与 API 返回均不代表完整转换成功。

## P1：功能完整性

- [ ] 修复 Snake 方向动作的固定坐标问题：应按当前蛇头位置连续移动，并更新尾部，不能每次都写入同一格；按用户要求单列待办，本次时钟修复不包含此项。
- [ ] 实际 Source 贪吃蛇连续移动、尾部更新、食物、增长、碰撞、计分和结束状态验证。
- [ ] 三维目标确实实现 Z 轴操作与三维规则，而非手写 fixture 替代模型输出。
- [ ] Design Intent 存在歧义、未解决项或待确认项时阻止继续 Lift。
- [ ] 中文输入/显示与多行需求编辑。
- [ ] 启用结构化输出，拆分单次四 IR 的大请求，修复适配器仅取首个文本块和丢失 usage 的问题；未变化的 Source 缓存复用。

## P2：界面与诊断体验

- [ ] Console 分类显示服务繁忙、连接故障、配置错误、规则校验错误，自动展开诊断。暂缓，已有 Diagnostics 可查，不优先于阻断修复。
- [x] 编译过程进度、取消、重复点击保护；已接生产 Workbench 主循环。仍需完整可见窗口操作回归。
- [ ] Source Plane 的视野、图例、控件提示与输入方式说明。

## 当前证据与集成范围

- 已切换到 `SRTP`；原有 Python 环境配置已恢复，并保留切分支前的 stash。
- 已用正式 Git 合并 `5f7e637` 将 `origin/llm` 的 `1feefe6cd3de795511e2075f54767cade750a26d` 整合进 `SRTP`。两条分支最初历史无共同祖先，但框架快照 `10ea353` 与 `9197133` 内容完全一致；本次合并保留两边历史，此后普通 merge 即可。手工复制的临时集成方式已取消。
- 15:07 Source 失败：模型 JSON 截断；四份 IR 与旧 Manifest 的哈希均不匹配。现场保存在历史测试目录的 `manual_test_records/approve-mismatch-20260922-150714`。
- 之前 Lift 失败：Gemini 返回 high demand。这与上述文件损坏是两个独立原因。

## 固定工作方式

只使用 `E:/CubeEngine/CubeEngine-SRTP`，日常保持 `SRTP`。更新前先提交本地开发改动；然后 `git fetch origin`、`git merge origin/llm`，在同一目录测试、继续修改。需要发布本地更新时推送 `origin/SRTP`。`main` 暂时不用，工程师的 `llm` 分支不承载本地修复。

旧 `work/llm-manual-1feefe6` 仅保留历史证据，不再作为启动入口；不再创建逐提交测试目录。
